{% extends "layouts/base.njk" %}

{% block content %}
    <h1>{{ title }}</h1>
    {% if content | toc %}
            <aside>{{ content | toc | safe }}</aside>
        {% endif %}
    {{ content | safe }}
{% endblock %}